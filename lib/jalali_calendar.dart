
export 'packages/jalali_calendar/src/jalali_calendar_picker.dart';
export 'packages/jalali_calendar/src/date_picker.dart';
export 'packages/jalali_calendar/src/persian_date.dart';

/*

  DatePicker.showDatePicker(context,
  initialDay: Jalali.now().day,
  initialMonth: Jalali.now().month,
  initialYear: Jalali.now().year,
  maxYear: Jalali.now().year+1,
  minYear: Jalali.now().year-1,
  onConfirm: (int? year,int? month, int? day){
if (year != null &&
                                      month != null &&
                                      day != null) {
                                    setState(() {});
                                  }
    }
  );

  */
